services:
  - type: web
    name: your-app-name
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist  # or ./build for CRA
    routes:
      - type: rewrite
        source: /*
        destination: /index.html